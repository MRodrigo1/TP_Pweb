@model TP_Pweb.Models.Empresa

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Empresa</h4>
    <hr />

   
        

    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Nome)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Nome)
        </dd>
     
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.avaliacao)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.avaliacao)
        </dd>
    </dl>

    
@*@if(ViewBag.FuncionariosSize != 0){

        @foreach (var item in ViewBag.FuncionariosEmpresa)
        {
            <tr>
                <td>
                    @if (item.Avatar == null)
                    {
                        <img class="img-thumbnail"
				             style="max-width:100px ; max-height:100px"
				             src="~/img/gestor.png" )">
                    }
                    else
                    {
                        <img class="img-thumbnail"
                            style="max-width:100px ; max-height:100px"
				            src="data:image/*;base64,@(Convert.ToBase64String(item.Avatar))">
                    }

                </td>
            </tr>
        }
    }else {
        <p>Existem 0 funcionarios</p>
        }*@
    
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.Id">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
